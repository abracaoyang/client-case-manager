# 專案特定開發規則 (Project Rules)

1. **禁止自動驗證與測試**：
   - 本主機所發出的所有指令與程式碼修改，**一律禁止**使用自動化瀏覽器代理（如 `browser_subagent`）或自動化測試指令進行驗證。
   - 所有視覺與功能性驗證，均由使用者手動測試，請勿消耗無謂的 Token。

2. **語音聽寫與快捷鍵聯動取消**：
   - 語音辨識、聽寫（Speech to Text）功能以及相關快捷鍵聯動（如 `Ctrl/Cmd + Shift + V`）已完全取消。
   - 系統中不需加入任何語音按鈕或對應的 API 綁定。

3. **「僅在必要時讀取、且僅讀取當前修改目標相關檔案」原則**：
   - 除非開發任務明確需要，否則**嚴禁**使用 `view_file` 讀取無關的輔助檔案（如 `Google_Sheet_串接指南.md`、`亮燈邏輯說明.md`、`README.md` 等）。
   - 開發目標僅鎖定在「客戶案件管理系統」的核心功能（如 `index.html`、`code.gs`）。
   - 讀取大檔案時，若只需修改特定區塊，應儘可能指定 `StartLine` 與 `EndLine` 進行區段讀取，避免載入整份檔案，以最大化節省 Token。

4. **系統核心 UI/UX 設計最高指導原則 [ADHD 成人業務專屬]**：
   - **空間應用與減法原則 (UX)**：
     - **禁止資訊稠密**：嚴格禁止塞滿無關緊要的儀表板數據。畫面只留下「當下這一秒需要處理」的核心元件。
     - **刪除與合併冗餘**：當新功能被要求時，優先檢查有無重複標籤（如階段名稱重複出現）。優先進行元件刪除、合併與重排簡化，降低使用者的認知負荷。
     - **不常駐顯示進階功能**：非核心欄位一律預設隱藏，僅在點擊、雙擊或特定狀態觸發時滑出（Slide），保持畫面極致淨空。
   - **排版與視覺階層 (UI)**：
     - **視覺整齊劃一**：所有輸入框（尤其是日期選擇器）、按鈕元件的寬度與高度必須「網格化（Grid）對齊」，不准出現高度不齊或高低落差。
     - **高對比度亮暗錨點**：底色採用極深色（暗黑模式），但核心行動按鈕（如完成、確定狀態）必須使用高對比度的亮色（如亮綠、亮橘），強迫視覺 0.1 秒聚焦。
   - **互動與防呆機制**：
     - **微互動反饋**：點擊成功必須有明顯的多巴胺即時反饋（如微動畫、色彩明顯跳轉）。
     - **微小阻力防呆**：針對涉及階段推移的高風險操作，一律實作雙擊（onDoubleClick/ondblclick）觸發，防止金手指誤觸。
     - **多模態輸入友善**：表單設計需同時包容快速點選（減少打字負擔），並配合系統既有規範。
